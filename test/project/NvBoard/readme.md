
## Cyber

将 Cyber 文件夹复制到 NJU-ProjectN/nvboard 项目下，与 nvboard 项目文件夹下， 与 example 同级目录。

进入 Cyber 文件夹，打开终端，输入：
```bash
make run
```

CyberSoftware 软件包的使用：终端输入：

```bash
make clean && make -j $(nproc) && make mem
```
